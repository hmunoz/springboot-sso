HOST_IP=10.114.81.191
#
RABBITMQ_DEFAULT_USER=rabbitmq
RABBITMQ_DEFAULT_PASS=rabbitmq
RABBITMQ_DEFAULT_VHOST=/
RABBITMQ_EXCHANGE=keycloak.events
# Keycloak
KEYCLOAK_PORT=9090
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin
# PostgreSQL
POSTGRES_DB=video
POSTGRES_USER=postgres
POSTGRES_PASSWORD=P4ssword!

# Gateway
# Servicios internos en red Docker videoclub
CATALOGO_URI=http://backend:8080
SOCIOS_URI=http://backend:8080
USERS_URI=http://backend:8080
NOTIFICACIONES_URI=http://backend:8080
AGENT_URI=http://agent:8085